(function() {
    "use strict";

    angular
        .module('app')
        .filter('recentUnique', function () {
            return function (items) {
                var filtered = [];
                for (var i = 0; i < items.length; i++) {

                }

                return filtered;
            };
        });
}());